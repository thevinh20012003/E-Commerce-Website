import { NycShopValidators } from './nyc-shop-validators';

describe('NycShopValidators', () => {
  it('should create an instance', () => {
    expect(new NycShopValidators()).toBeTruthy();
  });
});
